# 错误记录

## 2026-06-08：不得用规格审查替代编码循环

- 错误：实现子代理完成后，只派了规格审查子代理，没有按项目规范执行 `编码+测试 -> code review -> 修复 -> QA -> 修复 -> QA` 的完整循环。
- 规则：每个功能点完成 TDD 编码后，必须先派代码质量审查子代理；code review 不通过则派开发子代理修复并复审；code review 通过后再派 QA 子代理；QA 不通过继续修复并复审，直到明确“通过”。
- 禁止：使用“规格审查通过”“测试通过”替代 code review 或 QA。
